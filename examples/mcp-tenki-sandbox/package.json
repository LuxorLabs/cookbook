{
  "name": "mcp-tenki-sandbox",
  "private": true,
  "type": "module",
  "description": "Cookbook example — give Claude / Cursor a Tenki sandbox via the @tenkicloud/mcp MCP server.",
  "scripts": {
    "verify": "node verify.mjs"
  },
  "dependencies": {
    "@tenkicloud/mcp": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
